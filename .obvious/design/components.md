# Component manifest

AUTO-GENERATED by the `component-manifest-generator` skill v1.0.0 on 2026-08-21 for
`matthew-gerstman/blog`. Design system detected: **custom** (CSS Modules + CSS custom
properties; no component library).

The contracts below come from static analysis of component source. They are reviewed as
part of the pull request that introduces this file, not as a separate artifact — verify
any contract against the implementation before relying on it.

Notes on this repo's shape:

- Components are **flat files** (`src/components/Header.tsx`), not per-component
  directories. Each pairs with a sibling `Header.module.css`.
- No path aliases exist in `tsconfig.json` or `vite.config.js`, so `importPath` is the
  repo-relative module path; import it relatively from the consuming file.
- There is no Storybook/Ladle/Histoire and no story files, so every `storiesPath` is null.
- Every component is a named export except `Shimmer`, which is a default export.

```yaml
designSystem: 'custom'
componentRoot: 'src/components'
storyFormat: 'none'
tokenSource: 'css-variables'
components:
  - name: BlogPostRedirect
    importPath: 'src/components/BlogPostRedirect'
    storiesPath: null
    behavioralContracts:
      - 'Renders no UI — always returns a react-router `<Navigate replace />`. Mounting it navigates.'
      - 'Owns the legacy bare-slug fallback: a `:slug` matching a post redirects to `/writing/:slug`, anything else redirects to `/`.'
    antiPatterns:
      - 'Do not wrap it in layout chrome or render it alongside page content — it is a route-level side effect, not a view.'
    variants: []

  - name: CardHeader
    importPath: 'src/components/CardHeader'
    storiesPath: null
    behavioralContracts:
      - 'Renders a bare fragment, not a wrapper element — the parent owns the containing block and its layout.'
      - '`hasExpand` and `isExpanded` only control the +/− affordance glyph; the component holds no expansion state and attaches no handler.'
    antiPatterns:
      - 'Do not treat it as the expand control — the click target and open/closed state belong to the parent card.'
    variants: []

  - name: FindInPage
    importPath: 'src/components/FindInPage'
    storiesPath: null
    behavioralContracts:
      - 'Fully controlled: `isOpen` and `onClose` are owned by the parent; the component never self-closes without calling `onClose`.'
      - 'Moves focus into its input on open and handles its own `onKeyDown` — extend via that path rather than attaching competing document listeners.'
      - 'Drives `disabled` on its prev/next controls from match state; do not override.'
    antiPatterns:
      - 'Do not mount more than one instance — two search overlays fight over focus and over the same page text.'
    variants: []

  - name: GradientText
    importPath: 'src/components/GradientText'
    storiesPath: null
    behavioralContracts:
      - 'Polymorphic via `as` — renders the chosen tag so the caller controls document heading semantics.'
      - 'Concatenates `className` after its own class, so caller styles win on equal specificity.'
    antiPatterns:
      - 'Do not override `color` or `background-clip` from outside — that is the gradient mechanism, and overriding it renders the text invisible.'
    variants:
      - h1
      - h2
      - h3
      - span

  - name: Header
    importPath: 'src/components/Header'
    storiesPath: null
    behavioralContracts:
      - 'Theme is fully controlled — it renders `theme` and calls `onToggleTheme`; the `useTheme` hook owns persistence.'
      - 'Derives its own active-nav state from `useLocation`; it must render inside a router context.'
      - 'Subscribes to the sticky-job-title event bus (`src/utils/stickyJobTitle`) on mount and returns its unsubscribe — emitters must keep matching that contract.'
      - 'Sets `aria-label="Toggle theme"` on the icon-only theme button; do not drop it.'
    antiPatterns:
      - 'Do not render a second Header — both would subscribe to the sticky-title bus and race.'
      - 'Do not hardcode the active class from the parent; `isActive` already resolves it, including the `/` → Writing alias.'
    variants: []

  - name: JobCard
    importPath: 'src/components/JobCard'
    storiesPath: null
    behavioralContracts:
      - "Owns an `IntersectionObserver` at `threshold: [1]` with `rootMargin: '-100px'` to detect stick — that offset assumes the current header height."
      - 'Emits and clears global sticky-job-title events as a side effect of scroll position; unmount clears them.'
      - '`expandAll` and `targetProject` are pass-through hints to child ProjectCards, not local state.'
    antiPatterns:
      - 'Do not place it inside another scroll container — the observer is viewport-relative and stickiness will misfire.'
      - 'Do not change the header height without updating the `rootMargin` here.'
    variants: []

  - name: LinkList
    importPath: 'src/components/LinkList'
    storiesPath: null
    behavioralContracts:
      - 'Returns null for an empty or missing list — the caller must not add its own empty-state wrapper.'
      - 'Every link opens in a new tab with `rel="noopener noreferrer"`; that pairing is the security contract.'
    antiPatterns:
      - 'Do not use it for in-app navigation — it renders raw anchors, bypassing the router.'
    variants: []

  - name: MetaInfo
    importPath: 'src/components/MetaInfo'
    storiesPath: null
    behavioralContracts:
      - 'Filters falsy entries itself, so callers can pass conditional values directly rather than pre-filtering.'
      - 'Owns the bullet separator between items; it is rendered between entries, never around them.'
    antiPatterns:
      - 'Do not pass pre-joined strings with your own separators — you get double bullets.'
    variants: []

  - name: OptimizedImage
    importPath: 'src/components/OptimizedImage'
    storiesPath: null
    behavioralContracts:
      - 'Statically imports `public/images/optimized/manifest.json` at module scope. That file is generated and gitignored, so `npm run optimize-images` must have run or the whole app module fails to load.'
      - 'Progressive load: paints the thumbnail immediately, swaps to the requested size once preloaded, then flips the loaded class.'
      - 'Falls back to the raw `src` when the path is absent from the manifest — a missing image degrades, it does not throw.'
      - '`src` is matched against the manifest after stripping the `/images/blog/` prefix; paths outside that tree never resolve to an optimized asset.'
    antiPatterns:
      - 'Do not use a plain `<img>` for blog imagery — it skips the manifest and ships the full-size original.'
      - 'Do not hand-edit `public/images/optimized/` — it is regenerated wholesale by the optimize-images script.'
    variants:
      - thumbnail
      - medium
      - large
      - full

  - name: PostCard
    importPath: 'src/components/PostCard'
    storiesPath: null
    behavioralContracts:
      - 'Uses `index` for deterministic visual variety — it cycles shimmer intensity, duration, and direction. Reordering the list changes the styling.'
      - 'Computes reading time from `post.wordCount` via `calculateReadingTime`; `wordCount` is generated by `npm run calculate-wordcounts`.'
      - 'Routes to `post.externalLink` when present, otherwise to `/writing/:slug`.'
      - 'Reads hover geometry from a ref on mouse-enter to seed the shimmer angle; the card must be a positioned block.'
    antiPatterns:
      - 'Do not pass a constant `index` for every card — that collapses the intended variety.'
      - 'Do not hand-write `wordCount`; it is a generated field.'
    variants: []

  - name: ProgressBar
    importPath: 'src/components/ProgressBar'
    storiesPath: null
    behavioralContracts:
      - 'Takes no props — it reads scroll position straight from `useScrollProgress` and is inherently global.'
      - 'Expresses progress as inline `width`; the stylesheet owns everything else, including fixed positioning.'
    antiPatterns:
      - 'Do not mount it per-section expecting per-section progress — the hook measures the document.'
    variants: []

  - name: ProjectCard
    importPath: 'src/components/ProjectCard'
    storiesPath: null
    behavioralContracts:
      - 'Owns its expanded state internally; `shouldExpand` and `shouldScroll` are one-way hints applied through an effect, not controlled props.'
      - '`projectKey` is the deep-link identity used to match a targeted project — changing it breaks existing deep links.'
      - 'Scrolls itself into view via a ref when targeted, so the parent must not also scroll.'
    antiPatterns:
      - 'Do not treat `shouldExpand` as a controlled prop; user interaction overrides it and the component will not snap back.'
    variants: []

  - name: ProjectGridCard
    importPath: 'src/components/ProjectGridCard'
    storiesPath: null
    behavioralContracts:
      - 'Presentational and fully controlled — `onClick` is the only behavior, and the parent owns modal state.'
      - '`companyColor` is applied inline to the title, so it must be a valid CSS color, not a token name.'
    antiPatterns:
      - 'The click target is a `div` with no key handler or role — do not use it as the only path to content that has no other keyboard-reachable route.'
    variants: []

  - name: ProjectModal
    importPath: 'src/components/ProjectModal'
    storiesPath: null
    behavioralContracts:
      - 'Locks `document.body` overflow on mount and restores it on unmount — unmounting is what unlocks the page.'
      - 'Registers a document-level `keydown` listener for Escape and calls `onClose`; it never closes itself directly.'
      - 'Renders only when mounted — there is no `isOpen` prop, so the parent controls visibility by mounting and unmounting.'
      - 'Labels its close control with `aria-label`; keep that when restyling.'
    antiPatterns:
      - 'Do not hide it with CSS instead of unmounting — body scroll stays locked and the Escape listener stays live.'
      - 'Do not stack two modals — the second unmount restores an already-restored overflow value.'
    variants: []

  - name: ResponsiveEmbed
    importPath: 'src/components/ResponsiveEmbed'
    storiesPath: null
    behavioralContracts:
      - 'Aspect ratio is driven by the `data-aspect` attribute, which the stylesheet keys off — new ratios need both a type-union entry and a CSS rule.'
      - 'Owns the sizing box; children should fill 100% rather than setting their own width and height.'
    antiPatterns:
      - 'Do not set fixed pixel dimensions on the child iframe — that defeats the responsive container.'
    variants:
      - 16/9
      - 4/3
      - 1/1

  - name: SearchModal
    importPath: 'src/components/SearchModal'
    storiesPath: null
    behavioralContracts:
      - 'Controlled by `isOpen` / `onClose`; opened globally by the Cmd+K binding in `useKeyboard`.'
      - 'Focuses its input on open and owns arrow-key and Enter navigation through `onKeyDown` — extend that handler rather than adding a second listener.'
      - 'Navigates with `useNavigate` on selection, so it must render inside a router context.'
      - 'Searches across posts, projects, and talks passed in as props — it never fetches; the caller supplies the corpus.'
    antiPatterns:
      - 'Do not render it conditionally *and* pass `isOpen` — pick one, or focus management runs on a fresh mount every time.'
      - "Do not filter the corpus before passing it in; the component's own scoring expects the full set."
    variants: []

  - name: Shimmer
    importPath: 'src/components/Shimmer'
    storiesPath: null
    behavioralContracts:
      - "Default export, unlike every other component here — `import Shimmer from './Shimmer'`."
      - 'Polymorphic via `as`; renders the chosen element so heading semantics stay with the caller.'
      - 'Animation is CSS-driven from the position/animationType/intensity props; `duration` is seconds, not milliseconds.'
      - 'Also exports `ShimmerPosition`, `ShimmerAnimationType`, and `ShimmerElement` as the source of truth for those unions.'
    antiPatterns:
      - 'Do not wrap large subtrees — it is a text-decoration effect and animating a big subtree is expensive.'
      - 'Do not pass a millisecond `duration`; 2000 means 2000 seconds.'
    variants:
      - subtle
      - normal
      - bold

  - name: TagList
    importPath: 'src/components/TagList'
    storiesPath: null
    behavioralContracts:
      - 'Returns null for an empty or missing array — no wrapper is emitted, so parent gap styling will not leave a hole.'
      - 'Appends `className` after its own class, so caller layout overrides win.'
    antiPatterns:
      - 'Do not guard it with your own `tags?.length &&` check; the null return already handles it.'
    variants: []

  - name: TalkModal
    importPath: 'src/components/TalkModal'
    storiesPath: null
    behavioralContracts:
      - 'Locks and restores `document.body` overflow across its lifetime — unmount is what releases the page.'
      - 'Handles Escape through a document `keydown` listener and delegates to `onClose`.'
      - 'Visibility is mount-driven; there is no `isOpen` prop.'
      - 'Labels its close control with `aria-label`.'
    antiPatterns:
      - 'Do not keep it mounted and hidden — body scroll stays locked.'
      - 'Do not open it at the same time as ProjectModal; they share the same body-overflow global.'
    variants: []

  - name: TechRedirect
    importPath: 'src/components/TechRedirect'
    storiesPath: null
    behavioralContracts:
      - 'Renders no UI — unconditionally redirects `/tech/:slug` to `/writing/:slug` with `replace`.'
      - '`replace` is deliberate: the legacy URL must not land in history and trap the back button.'
    antiPatterns:
      - "Do not add validation here — an unknown slug is the article page's 404 to handle, not this component's."
    variants: []

  - name: TwitterEmbed
    importPath: 'src/components/TwitterEmbed'
    storiesPath: null
    behavioralContracts:
      - 'Renders null — it exists purely to inject `platform.twitter.com/widgets.js` and hydrate `.twitter-tweet` markup already in the DOM.'
      - 'Idempotent by script id: if `#twitter-wjs` is present it re-runs `twttr.widgets.load()` instead of adding a second script tag.'
      - 'Loads third-party script from an external origin, so embeds do not render offline or under a strict CSP.'
    antiPatterns:
      - 'Do not mount it before the tweet markup exists — `widgets.load()` only upgrades nodes already in the DOM.'
      - 'Do not add the widgets script directly in `index.html` as well; that reintroduces the duplicate this component guards against.'
    variants: []
```

## Generation summary

|                                              |                                                |
| -------------------------------------------- | ---------------------------------------------- |
| Repo                                         | `matthew-gerstman/blog`                        |
| Mode                                         | Create                                         |
| Design system                                | custom (CSS Modules + CSS custom properties)   |
| Component root                               | `src/components` (flat files, not directories) |
| Components found                             | 21                                             |
| With stories                                 | 0 (no story framework in this repo)            |
| Components missing >= 2 behavioral contracts | 0                                              |
| Components missing anti-patterns             | 0                                              |
| Monorepo packages                            | N/A                                            |
| Generated at                                 | 2026-08-21T17:30:00Z                           |

### Quality gates

- [x] All components have >= 2 behavioral contracts
- [x] All components have >= 1 anti-pattern
- [x] Import paths verified — no `tsconfig.json` or `vite.config.js` aliases exist, so paths are repo-relative
- [x] Story paths verified — no story files exist anywhere in the repo, so every `storiesPath` is `null`
